# MINI SHOP

Frontend-приложение, разработанное с использованием современных веб-технологий.  
Проект находится **в активной разработке** и **ещё не завершён**.


## 🚀 Запуск проекта

### 1. Клонировать репозиторий

```bash
git clone https://github.com/nnnozimjon/mini-shop
````

Перейти в папку проекта:

```bash
cd mini-shop
```

---

### 2. Установить зависимости

```bash
npm install
```

---

### 3. Настроить переменные окружения

В корне проекта создайте файл `.env` и добавьте необходимые переменные окружения:

```env
VITE_API_URL=http://localhost:3000
```

---

### 4. Запустить проект

```bash
npm run dev
```

После запуска приложение будет доступно по адресу:

```
http://localhost:5173
```

---

## 🛠 Используемые технологии

* React
* TypeScript
* Vite
* HTML / CSS
* REST API

---

## ⚠️ Статус проекта

❗ **Проект не является завершённым**

На данный момент предстоит ещё много работы, в том числе:

* Обновление и доработка **skeleton loaders**
* Улучшение и оптимизация **списков продуктов**
* Реализация **оформления заказа для гостя**
* Разработка и расширение **административной панели**
* Добавление новых функций и улучшение архитектуры
* Оптимизация **UX / UI**
* Исправление багов и рефакторинг кода

Проект будет активно дорабатываться.

---

## 📌 Примечание

Некоторые функции могут работать нестабильно или быть временно недоступны — это ожидаемо на текущем этапе разработки.

